FROM {{ .From }}

COPY {{ .CoredBinary }} /bin/{{ .CoredBinary }}
COPY {{ .CosmovisorBinary }} /bin/{{ .CosmovisorBinary }}

# FIXME (wojtek): Switch to ComovisorBinary in next step
ENTRYPOINT ["{{ .CoredBinary }}"]
